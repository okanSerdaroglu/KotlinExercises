package com.okan.kotlinexercises.functions

fun main() {

    var numbers = Array(10) { 0 }

    var charArray = Array(4) { "" }

    for (i in charArray.indices) { // this means i in 0..charArray.size -1
        println(i)
    }

    var names = arrayOf("okan", true, 6) // any kind of variables


}